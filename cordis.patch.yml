# dsh-codex-petcenter bundle patch: one insert row activating the pet-skin center plugin.
# Applied as part of the profile composition; the user's cordis.patch.yml can
# address this row by id for per-row overrides.
- insert:
    - id: codex-petcenter
      name: 'dsh-codex-petcenter'
